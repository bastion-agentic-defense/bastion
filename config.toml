# Per-transaction value cap (in whole units). The field name is retained for
# back-compat but is now chain-agnostic: it feeds the policy drift scanner,
# not a Solana outflow computation.
max_sol_per_tx = 1
rate_limit_per_minute = 120
allowed_programs = []
blocked_addresses = []
simulation_checks_enabled = true
