# BastionConfidentialGate - Formal Verification Spec (Certora)
#
# Proves the dual-layer gate invariants:
# 1. Execution requires BOTH CAPV verdict consumption AND Bastion policy pass
# 2. If either layer fails, execution reverts
# 3. Action commitment must match the verdict exactly
# 4. preflight() view function matches the state-changing checks
#
# Usage: certoraRun fv/evm/BastionConfidentialGate.conf

{
  "files": [
    "src/BastionConfidentialGate.sol",
    "src/interfaces/IBastionPolicy.sol",
    "lib/confidential-agent-policy-verdicts/src/IConfidentialPolicyVerdict.sol",
    "lib/confidential-agent-policy-verdicts/src/PolicyAction.sol"
  ],
  "verify": "BastionConfidentialGate:fv/evm/BastionConfidentialGate.spec",
  "solc": "solc-0.8.28",
  "optimistic_loop": true,
  "rule_sanity": "basic",
  "prover_args": [
    "-depth 10",
    "-mediumTimeout 90"
  ],
  "msg": "BastionConfidentialGate: dual-layer gate invariants"
}
