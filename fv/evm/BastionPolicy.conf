# BastionPolicy - Formal Verification Spec (Certora)
#
# Proves:
# 1. Policy rules cannot be bypassed by unregistered agents
# 2. Agent-specific limits are enforced per-agent, not globally
# 3. Policy deletion removes ALL restrictions
# 4. checkTransaction always returns consistent results for same inputs
#
# Usage: certoraRun fv/evm/BastionPolicy.conf

{
  "files": [
    "src/BastionPolicy.sol",
    "src/interfaces/IBastionPolicy.sol"
  ],
  "verify": "BastionPolicy:fv/evm/BastionPolicy.spec",
  "solc": "solc-0.8.28",
  "optimistic_loop": true,
  "rule_sanity": "basic",
  "loop_iter": 3,
  "prover_args": [
    "-depth 12",
    "-mediumTimeout 60"
  ],
  "msg": "BastionPolicy: enforcement correctness, deletion completeness"
}
